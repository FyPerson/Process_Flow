#!/bin/sh
# Git post-receive hook - Wrapper
# Delegates execution to the PowerShell script to handle Windows paths correctly

echo "Triggering PowerShell deployment script..."
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "hooks/post-receive.ps1"
exit $?
