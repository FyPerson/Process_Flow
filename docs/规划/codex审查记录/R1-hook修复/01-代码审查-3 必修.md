## 兜底审结论
- 总体：2 条遗漏 / 1 条新风险

## 必修项
`scripts/deploy.ps1:96-135`：PM2 验证、health 失败只 WARN，最后仍“部署完成”。这会制造新的假成功；应让 pm2 ssh 非 0、找不到行、uptime>=1m、health fail、mode!=production 都 `exit 1`。

`server-hooks/post-receive.ps1:62-138`：仓库模板仍是旧 ssh/npm/pm2 逻辑。必须同步为 fetch/reset/clean + 明确提示版。

`.claude/skills/deploy/SKILL.md:3,11-18,28,69-113`：主流程仍写 hook 自动部署/ssh/Deployment Complete，Gotcha #12 和正文互相打架。

## 建议项
`README.md:75,79` 明确三步：push origin、push server、跑 `powershell -File scripts/deploy.ps1`。前置检查建议改 `git ls-remote server refs/heads/main` 精确比 HEAD。

## R1 是否真根治了？
否。ssh 已搬出 hook，但模板/文档会把流程带回旧坑，且 deploy 验证失败仍可能绿字成功。