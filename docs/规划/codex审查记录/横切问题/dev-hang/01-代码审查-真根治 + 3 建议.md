## 审核结论
- 总体：通过，2 条非阻塞遗漏

## 必修项
无阻塞。

## 建议项
- `app.listen` 的 `error` 不进 `bootstrap.catch`，端口占用时可能跳过 `closeDb`。
- `initDb` 内部失败时 `dbInstance` 尚未赋值，`closeDb` 也覆盖不到。
- `dev` 建议加 `--kill-others-on-fail`。

## dev hang 是否真根治？
是：API 已不再等待嵌入式 Vite middleware。