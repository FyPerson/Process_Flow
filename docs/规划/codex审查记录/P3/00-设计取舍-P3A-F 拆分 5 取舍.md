## 阶段 3 整体方案
合理，但建议把 **P3B nanoid 推迟或夹在低风险窗口做**，不要阻塞权限主线。

## 推荐取舍
1. nanoid：**B，可推迟**。现有 ID 够用，P3 核心风险不在这里。
2. 前端权限：**C，A+B 组合**。前端友好提示，后端仍强校验。
3. 批注 UI：**D，A+B 组合**。气泡负责发现，详情 tab 负责完整操作。
4. admin：**A，独立 `/admin`**。权限边界清晰，后续扩展更稳。
5. 顺序：**A 或 C**。单人做选 `P3A → C → D → E → F → B`；多人可并行 P3E。

## 关键风险
最大风险是 **P3A saveCanvas delta 权限判断**：added/modified/removed、admin 例外、nodes_meta 同步、废弃路径容易漏边界。

其次是 **P3E 批注定位**：`sheet_id + node_id` 复合定位要从一开始做对。

P3F 风险在权限守卫，别只做 UI 隐藏。