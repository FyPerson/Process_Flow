# P5 Day 4 拍板记录 — 19 项决策锁定 + 实施切片清单

- **日期**：2026-05-07
- **依据**：[01-取舍审-Day4-真冲突UI.md](01-取舍审-Day4-真冲突UI.md) codex 取舍审 + 用户全部采纳推荐
- **canEnterImpl**：true / confidence: high

## 19 项决策（全部用户拍板锁定）

### 4 项用户预拍板（取舍审前置）

| # | 主题 | 拍板 | 落地约束 |
|---|---|---|---|
| **D-pre-1** | W1 弹窗按钮组 | 三按钮 = 保存草稿并查看最新版本 / 继续编辑草稿 / 取消 | 与 codex H1+H2+H5+X2 配合：三按钮各自语义独立，modal 不允许 ESC 隐式关闭 |
| **D-pre-2** | W2 mergeReport 透传 | 透传 + toast 显示概要 | 与 M1+X1 配合：走 SaveResult.merged 同步返回（report 必填 + appliedToLocal 字段） |
| **D-pre-3** | B 方案 changeSeq 不等淡化 toast | 显示 `已与对方改动合并，但你的最新编辑会在下次保存合并` | 与 M3+X1 配合：仅 deferred 分支（appliedToLocal=false）显示，session 内只提示一次 |
| **D-pre-4** | W3 e2e 范围 | 3 路径（直接保存 / 真合并自动通过 / 真合并冲突弹窗） | 与 H3 配合：复用 P3G 真服务端 + 双 browser context 模式 |

### 5 项 high 风险（codex 揪出 → 用户采纳推荐）

| # | 关联 | 主题 | 推荐方案 | 落地约束 |
|---|---|---|---|---|
| **D-1** | H1 / J2 | 保存草稿按钮主动 PUT draft | 选项 B | 按钮 onClick：`await apiPutDraft(currentProject)` 成功后 `discardAndReload`；PUT 失败保留弹窗 + dirty 不 reload |
| **D-2** | H2 / J3 | 继续编辑草稿恢复 autosave | 语义 Y | onClick：`setConflict(null)` + `setAutoSaveDisabled(false)`；**不动** serverVersionRef / projectRef / dirty / 草稿（B 方案不变量） |
| **D-3** | H3 / J8 | e2e 真服务端真合并 | 选项 A | tmp-test/ 复用 P3G dev server 模式，2 个 browser context（A/B 用户）操作同一 public canvas 跑 3 路径 |
| **D-4** | H4 / J9 | #34(g) hook 层断言不变量 | 选项 A + 轻量 g3 grep | useMultiCanvas 单测覆盖 g1（状态不变断言）+ g2（两轮保存）；g3 用 npm script 跑 grep 扫"merged + changeSeq 不等"分支 |
| **D-5** | H5 / J11 | 弹窗 modal block | 选项 A | aria-modal=true，ESC 不隐式关闭；CanvasSwitcher/Import/beforeunload 继续受 `dirty \|\| saving \|\| conflict` 保护 |

### 5 项 medium 风险

| # | 关联 | 主题 | 推荐方案 | 落地约束 |
|---|---|---|---|---|
| **D-6** | M1 / J1 | SaveResult.merged 加必填 report | 选项 A | `SaveResult` 收紧：`{ status: 'merged'; version; mergedFromVersion; report: MergeReport; appliedToLocal: boolean }`（X1 同步集成） |
| **D-7** | M2 / J5 | E7 warning toast 可展开详情 | 选项 B | toast 概要"已合并 N 条 + 丢弃 K 条悬空边"，展开区列 sheetId/edgeId/missingEndpoint，能跳转跳转，不能也可复制定位 |
| **D-8** | M3 / J6 | changeSeq 不等 toast 去重 | 选项 C | session 内首次提示一次；正常 saved/merged-applied/reload/切画布后重置去重标记（用 useRef 持有 boolean flag） |
| **D-9** | M4 / J7 | base_version_expired 独立弹窗 | 选项 A | 新建 `BaseVersionExpiredDialog`：主按钮"保存草稿后重载"先 PUT draft 再 reload；"留在草稿"可关闭但**不**恢复 autosave |
| **D-10** | M5 / J12 | 弹窗 + toast React Portal | 选项 A | `ReactDOM.createPortal` 挂 document.body；少动画 + 不用 backdrop-filter；50 条 conflicts 用 max-height + scroll，不做 virtualize |

### 2 项 low 风险

| # | 关联 | 主题 | 推荐方案 | 落地约束 |
|---|---|---|---|---|
| **D-11** | L1 / J4 | toast 自建 | 选项 A | 新建 `src/components/Toast/{ToastHost,ToastItem}.tsx`，参考 DraftRecoveryDialog CSS；支持 info/warning + 替换/去重 + 可展开 |
| **D-12** | L2 / J10 | conflicts undefined fallback | 选项 B | ConflictResolutionDialog 仍显示三按钮，详情区："服务端未返回冲突明细，建议先保存草稿后查看最新版" |

### 3 项 codex 主动挖出（X1-X3）

| # | 关联 | 主题 | 落地约束 |
|---|---|---|---|
| **D-13** | X1 | SaveResult.merged 加 appliedToLocal 字段 | 服务端不动；客户端 `apiSaveCanvas` 解析 `merged=true` 后判 changeSeqRef.current === capturedSeq 设 `appliedToLocal: boolean`（与 D-6 同步） |
| **D-14** | X2 | "取消"按钮顶栏提示可重开 | 取消 onClick：关弹窗保留 conflict state；BFV SaveStatus 加"冲突待处理 (vN)"提示 + 点击重开 ConflictResolutionDialog |
| **D-15** | X3 | 弹窗 pending 中 disable 全按钮 | ConflictResolutionDialog 内部 `actionPending: boolean` state；任一按钮触发 await 期间所有按钮 disabled |

## 与 Day 1/2/3 已锁定不变量的对齐

### B 方案核心不变量（Day 3 三审 PASS）

`serverVersionRef.current ≡ projectRef.current 的服务端基线`

Day 4 影响点：
- ✅ D-2 "继续编辑草稿"语义 Y：**不动 serverVersionRef / projectRef / dirty / 草稿** —— 完整保不变量
- ✅ D-13 appliedToLocal 字段：与 useMultiCanvas merged 分支 changeSeqRef 判断同步，不引入新状态
- ✅ D-4 #34(g) g1 单测：直接断言 4 个 ref/state 在 changeSeq 不等时不动

### conflict_logs 三种 resolution（Day 3 D-2 锁定）

`auto_merged` / `conflict` / `base_version_expired` —— 服务端已写入，Day 4 客户端不改

### 契约测试守门（Day 3 D-3 锁定）

`Assert<T extends true>` + `IsEqual` + `IsBidirectionalAssignable` 真 compile-fail pattern

Day 4 影响点：
- D-6 SaveResult 收紧 + D-13 appliedToLocal 加字段 → 客户端 `canvases.types.ts` 同步更新 + 跑契约测试反向验证（删字段必须 tsc 报错）

## 实施切片清单（codex 切片设计审重排版）

> **codex 切片设计审反馈**：原顺序经 codex 审，14 项意见全部采纳，新增 F-3.5 hook API 封装切片 + 重排 F-15/F-16 + e2e 后置 + 三个文案落点明写。详见 [03-切片设计审.md](03-切片设计审.md)。

### 切片 1 — 类型层（Day 4 启动基础）

- **F-1**（M1 调整）：**确认服务端 SaveCanvasResult 不加 appliedToLocal**（守门切片，不改代码）。`appliedToLocal` 是客户端 hook 层语义，服务端继续返 `{ merged: true; mergedData; mergedFromVersion; report }` 不变；客户端 hook `SaveResult.merged` 才加 appliedToLocal（D-13）
- **F-2**：`src/api/canvases.types.ts` 客户端 API 镜像保持不变（已有 `report: MergeReport`）—— 客户端 `SaveResult` 与 API 层 `SaveCanvasResultClient` 是不同边界，F-3 不反向影响 F-2
- **F-3**：`src/hooks/useMultiCanvas.ts` `SaveResult` 收紧：`{ status: 'merged'; version; mergedFromVersion; report: MergeReport; appliedToLocal: boolean }`（D-6 + D-13）
- **F-3.5**（H1 新增切片 — codex 拦下的关键缺口）：useMultiCanvas 暴露**命令式封装** `clearConflictAndResumeAutosave()`（D-2 落点），ConflictResolutionDialog onContinueEdit/onCancel 调用此封装而不直接拼 setConflict + setAutoSaveDisabled；避免 BFV/Dialog 绕过 hook 边界
- **F-4**（M2 调整）：`src/api/canvases.types.contract.test.ts` 范围明确**只验证 server/client API shape + MergeReport**；hook 层 `SaveResult.merged.report` + `appliedToLocal` 必填断言放 F-3 或 F-6

### 切片 2 — useMultiCanvas hook 改造（B 方案不变量守门）

- **F-5**（M3 调整）：merged 分支**进入立即冻结** `appliedToLocal`（在任何 await / setState 前）：
  ```ts
  // merged=true 分支入口
  const appliedToLocal = changeSeqRef.current === capturedSeq;
  // 后续只用该 frozen boolean，绝不二次读 changeSeqRef.current
  ```
- **F-6**：单测 `useMultiCanvas.test.ts`（如未存在则建）补 g1（状态不变断言：merged + changeSeq 不等时 4 ref/state 不动）+ g2（两轮保存：第一轮 deferred 第二轮 applied 双方改动都进 mergedData）（D-4）
- **F-7**（M4 调整）：`scripts/check-b-plan-invariant.mjs` CI grep 脚本，扫 `src/hooks/useMultiCanvas.ts` 验证 merged 分支 changeSeq 不等路径无推进 ref / 替换 projectRef（D-4 g3）+ npm script `check:invariants` 接入 Day 4 验证命令链（不一定进 npm test，独立可调）

### 切片 3 — Toast 基建 + 弹窗组件

- **F-8**（H2 调整 + M7 + A2 调整）：`src/components/Toast/{ToastProvider,ToastHost,ToastItem}.tsx` + `styles.css`（D-11）。**采用 ToastProvider + useToast() hook 模式**（不是全局 Toast.show()）；React Portal 挂 document.body（D-10）；支持 info/warning + key 替换（通用去重，**不**做业务级 session once 去重 — 那是 F-11 BFV deferredMergeToastShownRef 的事）；应用根挂 `<ToastProvider>`
- **F-9**（M7 + L2 调整）：`src/components/ConflictResolutionDialog/{index,styles}` 三按钮 + 4B 流程（D-pre-1 + D-1 + D-2 + D-5 + D-7 + D-12 + D-15）。**明确写 conflicts undefined fallback 文案**："服务端未返回冲突明细，建议先保存草稿后查看最新版"（D-12）
- **F-10**（A1 调整）：`src/components/BaseVersionExpiredDialog/{index,styles}` 独立弹窗（D-9）。**props 区分** `onSaveDraftAndReload` 与 `onStayInDraft`：onStayInDraft **只关弹窗 + 不调用恢复 autosave**（A1 调整，与 D-9 对齐"留在草稿不恢复 autosave"）

### 切片 4 — BFV 接弹窗 + 顶栏提示

- **F-11**（M7 + A2 调整）：BFV `handleSave` `case 'conflict'` 替换 confirm 为 `<ConflictResolutionDialog>`；`case 'merged'` 加 toast（**消费 hook SaveResult 不是 API raw**，L1）：
  - `appliedToLocal=true` 分支 → toast：`已合并 [对方username] 的 N 条改动`（含 E7 warning 计数 + 可展开详情 D-7）
  - `appliedToLocal=false` 分支（B 方案 deferred）→ toast 文案明写：`已与对方改动合并，但你的最新编辑会在下次保存合并`（D-pre-3）
  - **session 内只提示一次** deferred toast：BFV 持有 `deferredMergeToastShownRef = useRef(false)`（D-8 + A2）；正常 saved/applied/reload/切画布时重置为 false
  - `case 'base_version_expired'` 替换 alert 为 `<BaseVersionExpiredDialog>`（D-9）
- **F-12**（H3 调整）：BFV 在 SaveStatus **附近追加独立 pendingConflict 操作 UI**（不进 SaveStatus 内部状态机）—— "冲突待处理 (vN) 点击重开"按钮。SaveStatus 可接收 conflict badge prop 但不持有弹窗状态；重开弹窗逻辑由 BFV 控制（D-14）
- **F-13**（L2 调整）：BFV `handleImport`/`handleSwitchCanvas`/beforeunload 校验 `conflict` 现状已存在（不需改）。**补轻量 grep/断言**验证三处都含 `conflict` 判断（防回归 L2）；加注释说明 modal 期间这些路径受保护（D-5）

### 切片 5 — 测试债务兑现（M5 调整：先服务层后路由层 / 04 范围判读审拆 F-16 三段）

> **04 范围判读审 2026-05-08 拍板 4 条**（详见 [04-F16-fh范围判读审.md](04-F16-fh范围判读审.md)）：
> 1. (f)/(h) 整体路线 = 第三条 / A+ — **不引入 RTL/jsdom**
> 2. (f) 承载 = 抽 save 副作用 dispatcher，node --test 跑分支
> 3. (h) 承载 = 同款 dispatcher（输出"是否调 apiDeleteDraft" boolean）
> 4. F-16 验收口径补**子断言 → 具体落点映射表**（见下表）

- **F-16a**（服务端端到端 5 case）：`server/services/canvases.merge.test.ts` 补 (a)/(b)/(c)/(d)/(e) — annotations 级联删除 / sheet_removed_modified 副作用型 / deprecated 路径合并 / E7 warnings 流入 mergeReport / connector 自动合并 E1/E2/E3
- **F-16b**（客户端 dispatcher 抽取 + 测试）：抽 save 结果副作用 dispatcher（小重构，~30 min）+ 新建 `src/hooks/saveResultDispatcher.test.ts` 跑 (f)/(h) 子断言
- **F-16c**（验收口径明文 + 映射表）：本段下方插入**子断言 → 测试落点映射表**；同步更新技术债务登记 #34 修法段写明"#34 (f)/(h) 闭环条件 = F-16b dispatcher 测试通过 + F-14b e2e 通过；不接受注释 / grep 单独关闭"
- **F-15**（M5 顺序调整 — 后置）：`server/routes/canvases.test.ts` PUT 路由层 supertest 复测 #33（DataIntegrityError 500 + conflicts JSON 序列化）— 依赖 F-16 服务层错误形状已稳定

#### F-16 子断言 → 测试落点映射表（codex L1 + recommendation 2 必修）

| #34 子项 | 内容 | 服务端 case | hook 纯函数 | dispatcher 测试 | contract 测试 | e2e (F-14b) |
|---|---|---|---|---|---|---|
| (a) | annotations 级联删除 | F-16a case 1 | — | — | — | — |
| (b) | sheet_removed_modified 副作用型 | F-16a case 2 | — | — | — | — |
| (c) | deprecated/alsoDeprecated 路径 | F-16a case 3 | — | — | — | — |
| (d) | E7 warnings 流入 mergeReport | F-16a case 4 | — | — | — | — |
| (e) | connector 自动合并 E1/E2/E3 | F-16a case 5 | — | — | — | — |
| (f1) | merged=true 状态替换 | — | mergeSavePlan g1/g3 | F-16b case 1 | — | F-14b 路径 2 |
| (f2) | base_version_expired 重载 | — | — | F-16b case 2 | — | F-14b 路径 3 |
| (f3) | 409 conflict 携 conflicts | — | — | F-16b case 3 | F-4 contract | F-14b 路径 3 |
| (f4) | saving 期间 changeSeq 检查 | — | mergeSavePlan g2 | — | — | — |
| (f5) | canvasId 切换 discarded | — | — | F-16b case 4 | — | F-14b（隐式覆盖）|
| (h) | discardAndReload 不删草稿 | — | — | F-16b case 5 | — | F-14b 路径 3 |

> **闭环判定**：上表每行至少一个具体落点 ✓ 才算偿还；空行（如 (a)-(e) 仅服务端 case）必须有该 case 真测过 + 通过验证命令链才算关闭。
> **不接受**：注释解释 / grep 静态守门作为唯一关闭证据。

### 切片 6 — Playwright e2e（H4 调整：前置 smoke）

- **F-14a**（H4 新增 — 前置 smoke）：`tmp-test/test-p5-day4-double-context-smoke.py`：双 browser context 打开同一 canvas，验证读到相同版本 + localStorage/cookie 隔离 + 独立 seed；smoke 通过才进 F-14b
- **F-14b**（H4 主体 — 06 设计审范围调整）：`tmp-test/test-p5-day4-conflict-flow.py` 3 路径 e2e。**范围调整**（详见 [06-F14b-设计取舍审.md](06-F14b-设计取舍审.md) codex 拍板）：F-14b 不再验证"双方浏览器完整协作"，改为**"单真实浏览器客户端（user01）+ admin 通过 API 并发写入"**——双 ctx 隔离能力由 F-14a 覆盖，F-14b 聚焦"真实浏览器保存路径触发后的 Toast / ConflictResolutionDialog UI 响应"。

  **覆盖矩阵**：
  | 测试 | 覆盖范围 |
  |---|---|
  | F-14a | 双 ctx / token / version 隔离基建 |
  | mergeSavePlan + saveErrorDispatcher 单测 | 客户端 plan 决策分支 |
  | canvases.merge.test.ts (F-16a) | 服务端合并端到端 |
  | canvases.put.test.ts (F-15) | route 层 supertest |
  | **F-14b** | **真实浏览器客户端 + 服务端并发写入后的 UI 响应（Toast / 弹窗）** |

  **3 路径**：
  - **路径 1 直接保存**（独立 setup，无 admin API 推送）：baseVersion=N / currentVersion=N → 保存后 DB version=N+1；后置 3 断言（SaveStatus 回到已保存 / 服务端 version 推进 / 刷新可见）
  - **路径 2 真合并自动通过**：admin 改节点 A label / user01 改节点 B label（不同节点同字段 → 不重叠）→ Toast 显示"自动合并..." + 不弹冲突弹窗
  - **路径 3 真冲突弹窗**：admin 和 user01 改同一节点 label → ConflictResolutionDialog 弹出含冲突列表

  **降低 flaky 手段**（codex risks 段）：唯一 canvasId / 每步读服务端 version 断言 / 避免固定 5 秒等待 / **明确按钮保存优先于等待 autosave** / 保存前断言 dirty + version=N+1

### 切片 7 — codex 末尾审 + 部署（M6 + L3 调整）

- **F-17**（M6 调整）：codex 末尾审（advice-only / canEnterBump 门禁）。**末尾审前必跑完整验证链**：`npm test` + tsc 三端 + lint:ids + `check:invariants`（F-7 grep）+ Day 4 Playwright e2e（F-14b）；e2e 不进 npm test，文档单列命令和结果
- **F-18**：bump v1.15.0（Day 3 v1.14.0 未 deploy → Day 4 真部署一并上）
- **F-19**（L3 调整）：deploy.ps1 不 -SkipBuild（前端 src 大量改动）→ 三端 HEAD 同步 + PM2 重启 + /api/health 验证。**部署前通知 5 人短暂停止编辑**（PM2 重启 ~10s 窗口可能打断保存），等 /api/health 通过后再恢复
- **F-20**：99-收尾.md（Day 4 7 件套文档同步：99-收尾 + neat-freak 模式 B 阶段发版三件套）

## 切片重排后的执行顺序（codex 切片设计审建议 + 用户采纳）

```
F-1 → F-2 → F-3 → F-3.5 → F-4 → F-5 → F-6 → F-7 → F-8 → F-9 → F-10 → F-11 → F-12 → F-13 → F-16a → F-16b → F-16c → F-15 → F-14a → F-14b → F-17 → F-18 → F-19 → F-20
```

**核心调整**（codex 拦下的 4 高 + 7 中 + 3 低 + 2 主动挖共 16 项问题）：
1. **加 F-3.5 hook API 封装切片**（H1）— 避免 ConflictResolutionDialog 绕过 hook 边界直拼 setter
2. **F-8 ToastProvider + useToast() hook 模式**（H2）— 不是全局 imperative
3. **F-12 SaveStatus 不吞并状态**（H3）— 独立 pendingConflict UI
4. **F-14a 双 context smoke 前置**（H4）— 验证 P3G 模式能愉双 context
5. **F-1 改为守门切片**（M1）— 服务端不加 appliedToLocal
6. **F-5 冻结 appliedToLocal**（M3）— 进 merged 分支立即冻结避免异步漂移
7. **F-15/F-16 顺序对调**（M5）— 先服务层后路由层
8. **F-7 grep 接入 Day 4 验证命令**（M4）— 不一定进 npm test
9. **F-17 末尾审验证命令链补全**（M6）— 必跑 e2e + check:invariants
10. **三个文案/标记落点明写**（M7）— D-pre-3 deferred 文案 / D-8 useRef session once / D-12 conflicts undefined fallback
11. **F-10 BaseVersionExpiredDialog props 区分**（A1）— onSaveDraftAndReload vs onStayInDraft
12. **F-11 BFV 持有 deferredMergeToastShownRef**（A2）— Toast 基建只做通用 key replacement
13. **F-11 消费 hook SaveResult 不是 API raw**（L1）
14. **F-13 补轻量 grep/断言验证 conflict 保护**（L2）
15. **F-19 部署前通知 5 人**（L3）
16. **F-4 contract test 范围明确**（M2）— 只守 API shape，hook 层断言放 F-3/F-6

## Day 4 启动前的目录结构（已就位）

```
docs/规划/codex审查记录/阶段5/P5-合并算法/Day4-真冲突UI/
├── 01-取舍审-Day4-真冲突UI.md   ← 本次 codex 取舍审 + Claude 拍板
├── 02-拍板记录.md                ← 本文档（19 项决策锁定 + 实施切片清单）
└── （后续按需追加 03-代码审-XX / 04-末尾审-首轮 / [复审] / [三审] / 99-收尾）
```

## 与阶段 5 整体进度的关系

- 阶段 5 进度：3/4 → 完工后 4/4（最后 1 子阶段）
- 整阶段 22 项决策（Day 1: 11 / Day 2: 11 / Day 3: 5）+ Day 4 19 项决策 = **41 项决策锁定**
- 不变量传递：Day 1 → Day 2 → Day 3 → Day 4 全部继承（B 方案 / conflict_logs 三 resolution / 契约测试守门 / parentId 真校验）

## 下一步

按切片 1 → 切片 6 顺序实施。预估耗时（按方案 §7）：1 整天。

**实施前用户确认**：是否要在切片 1 开始前再走一轮 codex 设计审（advice-only），还是直接进切片 1 的代码实施？
