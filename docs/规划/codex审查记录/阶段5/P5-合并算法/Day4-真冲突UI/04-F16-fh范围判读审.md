# P5 Day 4 F-16 (f)/(h) 范围判读审 — codex spec-critique

- **日期**：2026-05-08
- **触发**：F-16 起手时发现两份文档对 (f)/(h) 子项的承载方式描述对不上：
  - 文档 A（02-拍板记录 切片设计审 § F-16）：明列 (f) D-4 hook 单测 + (h) discardAndReload 不删草稿断言
  - 文档 B（技术债务登记 #34 修法）：原文 *"e2e 跑真画布 PUT 真合并路径，比 mock fetch + React renderer 写 hook 单测更直接"*
- **codex 调用**：
  - mode：advice-only / spec-critique
  - sandbox：read-only
  - prompt：[E:/tmp/codex-day4-f16-fh-judge.md](file:///E:/tmp/codex-day4-f16-fh-judge.md)（5.8KB）
  - 同步阻塞跑 ~45s 返回
  - codex-cli 0.128.0
  - 输出 wrapper：[codex_spec-critique_20260508_000119.json](file:///C:/Users/FY/AppData/Local/Temp/codex-bridge-workspace/runs/codex_spec-critique_20260508_000119.json)
- **整体判定**：codex 推荐 **第三条 / A+**（不引入 RTL/jsdom，但不只用注释 + grep 关闭债务）/ confidence: high

## 第一层 — 非技术总结

调 F-16 起手发现 (f) hook 单测 / (h) discardAndReload 断言两个子项有"按 RTL 写 / 按 e2e 替代 / 按注释关闭"三种判读分歧。我向 codex 请第二视角。codex 拦下了"用注释关闭"和"用 grep 关闭"两条偷工路径，但同意"不引入 RTL/jsdom"——结论是用 Node `--test` 跑**薄副作用执行器或纯函数抽取**的可执行断言，让债务闭环既低成本又有真测试证据。

## 第二层 — codex 5 项原话技术细节

### High — 2 条

**H1 — ambiguity / 文档 A vs 文档 B #34**
- problem：A 把 (f)/(h) 放进 F-16"补测试"清单，B 的修法又明确倾向 e2e 替代 hook 单测；当前没有定义什么证据可以算 #34 (f)/(h) 真正关闭。
- suggestion：在 F-16 验收口径里明确改成"不得新增 RTL/jsdom；(f) 由 mergeSavePlan 纯函数测试 + F-4 contract + F-14b e2e + 必要的 Node 层副作用薄测试共同关闭；(h) 必须有可执行断言证明 base_version_expired 后 draft 保留"。

**H2 — missing-constraint / #34 (f)**
- problem：现有 mergeSavePlan 只覆盖 5 个子断言中的部分不变量，直接用注释说明"三层共同覆盖"不足以关闭"base_version_expired 重载 / 409 conflict 携 conflicts / canvasId 切换 discarded"等副作用层风险。
- suggestion：保留纯函数路线，但补一个无需 React renderer 的薄封装测试：把 save 结果分支处理或副作用 dispatcher 抽成可注入函数，用 node --test 验证 5 个子断言中未覆盖的关键分支。

### Medium — 2 条

**M1 — edge-case / #34 (h) discardAndReload**
- problem：grep 验证 BFV handleBaseVersionExpired 不调用 apiDeleteDraft 不是稳定测试：删除草稿可能经由别名、包装函数、重命名 API 或后续重构绕过，且不能证明 base_version_expired 后 drafts 实际仍保留。
- suggestion：不要把 grep 作为主闭环。更合理的是写可执行断言：mock API 层或状态层，触发 base_version_expired/discardAndReload 路径后断言 draft store/API delete 未发生且本地 drafts 仍存在。若短期只能 grep，应标成临时 smoke guard，不能标成完全偿还。

**M2 — performance / 反向选项 B**
- problem：为少量 hook 行为引入 RTL + jsdom 会改变测试基建形态，后续会诱导更多 React renderer 测试进入项目；对当前 node --test、单实例、低并发 MVP 来说维护成本偏高。
- suggestion：除非未来会系统性测试大量 React hooks，否则不要为 F-16 单独引入这套依赖。优先抽纯函数或薄副作用执行器，用现有 node --test 跑。

### Low — 1 条

**L1 — readability / F-16 切片清单**
- problem：A 中 F-16 目标文件写的是 server/services/canvases.merge.test.ts，但当前事实中 (f) 已落在 src/hooks/mergeSavePlan.test.ts，服务端测试与客户端 hook 测试边界容易混淆。
- suggestion：在切片记录里拆清楚：服务端 merge 测试覆盖哪些子项 / 客户端 hook/save 计划测试覆盖哪些子项 / e2e 覆盖哪些集成路径。

### recommendations / risks / notes_for_claude_code

详见 [wrapper JSON 第 23 行 finalMessage 字段](file:///C:/Users/FY/AppData/Local/Temp/codex-bridge-workspace/runs/codex_spec-critique_20260508_000119.json)。

## 第三层 — Claude 判断 / 用户决策 / 落地动作

### 用户拍板（2026-05-08）

按 codex 推荐 + Claude 建议**全采纳**：

| # | 拍板 | 落地 |
|---|---|---|
| 1 | F-16 (f)/(h) 整体路线 = **第三条 / A+** | 不引入 RTL/jsdom；不只用注释 + grep 关闭债务；用 Node --test 跑薄副作用 dispatcher / 纯函数抽取 |
| 2 | (f) 承载 = 抽 save 结果**副作用 dispatcher** | 在 useMultiCanvas.save() 内部把 4 种 SaveResult 分支（merged-apply / merged-defer / base_version_expired / conflict）的副作用决策抽成可注入纯函数；node --test 跑分支覆盖 |
| 3 | (h) 承载 = **选 H**（与 (f) 同款 dispatcher） | dispatcher 输出"是否调 apiDeleteDraft"的 boolean；base_version_expired 路径断言 false。比"选 G 单独抽 shouldDeleteDraftOnDiscard 纯函数"更覆盖 |
| 4 | F-16 验收口径补**子断言→具体落点映射表** | 在 02-拍板记录 § F-16 段插入一张映射表，把 (a)-(h) 7 个子项每个对应到（服务端 case # / mergeSavePlan 测试 # / contract 测试 # / e2e 编号 / dispatcher 测试 #）的具体落点 |

### codex 5 项意见 → Claude 落地映射

| codex 意见 | 严重度 | 处理 |
|---|---|---|
| H1 ambiguity（验收口径未定义） | high | ✅ 拍板 4 — 补映射表 + F-16 验收口径写明"不得新增 RTL/jsdom；(f)/(h) 闭环要求" |
| H2 missing-constraint（注释关闭不足） | high | ✅ 拍板 2 — 抽 save 副作用 dispatcher，node --test 覆盖 |
| M1 edge-case（grep 不稳定） | medium | ✅ 拍板 3 — (h) 由 dispatcher 可执行断言闭环，grep 仅作 smoke guard 不替代 |
| M2 performance（B 路线否决） | medium | ✅ 拍板 1 — 全程不装 RTL/jsdom |
| L1 readability（服务端/客户端边界混淆） | low | ✅ 拍板 4 — 映射表里按测试位置分列服务端 / hook / contract / e2e / dispatcher |

### 落地切片新增 / 调整

- **F-16 拆分细化**：
  - F-16a 服务端 `canvases.merge.test.ts` 补 (a)/(b)/(c)/(d)/(e) 5 个端到端 case
  - F-16b 抽 save 副作用 dispatcher（小重构）+ 新建 `src/hooks/saveResultDispatcher.test.ts` 跑 (f)/(h) 子断言
  - F-16c 在 02-拍板记录 § F-16 加映射表 + 在 #34 修法段更新闭环口径

- **预估**：原 F-16 1.5-2h → 调整后 2-2.5h（增加抽 dispatcher 的 ~30 min 小重构 + 映射表 ~10 min）

### 风险吸收

- codex risks 段提到 *"薄抽取可能比加注释慢"* —— 接受这个风险，因为债务真闭环的价值远高于 30 min 重构成本
- codex risks 段提到 *"按 A 执行的债务闭环证据不足"* —— 通过拍板 2/3 的 dispatcher 测试 + 拍板 4 的映射表两层兜住

