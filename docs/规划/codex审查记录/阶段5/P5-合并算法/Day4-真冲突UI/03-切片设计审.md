# P5 Day 4 切片设计审 — 实施切片清单 codex 审查 + 用户拍板

- **日期**：2026-05-07
- **依据**：[02-拍板记录.md](02-拍板记录.md) 19 项决策锁定后，对 20 项实施切片（F-1 ~ F-20）的设计级审查
- **codex 调用**：
  - 模式：advice-only / sandbox read-only / MaxContextChars=200000
  - 调用方式：stdin 注入（17.8KB prompt，含 02-拍板记录.md 全文）
  - 同步阻塞跑（~60-180s，实际 ~90s 返回）
  - 输入文件：`E:/tmp/codex-day4-qiepian-shen.md`
  - 输出文件：`E:/tmp/codex-day4-qiepian-result.md`
- **整体判定**：**canEnterImpl: true / confidence: medium**（取舍审 high → 设计审 medium，因为切片清单有 16 项可优化点）
- **结论**：建议重排切片顺序 + 加 F-3.5 + 调整若干 F-X 描述

## codex 16 项意见 + 用户拍板（全部采纳）

### High（4 条）

| # | 触发 | 主题 | 用户拍板 | 落地 |
|---|---|---|---|---|
| **H1** | Q6 | F-9/F-11 缺 hook API 边界 | ✅ 采纳 — 加 F-3.5 切片 | useMultiCanvas 暴露命令式 `clearConflictAndResumeAutosave()`；ConflictResolutionDialog 不直接拼 setter |
| **H2** | Q5 | F-8 Toast 接口未定 | ✅ 采纳 ToastProvider + useToast() hook 模式 | 不用全局 Toast.show()；BFV 应用根挂 `<ToastProvider>` |
| **H3** | Q7 | F-12 SaveStatus 不应吞并保存状态 | ✅ 采纳 — BFV 在 SaveStatus 附近追加独立 pendingConflict UI | SaveStatus 不持有弹窗状态；可接收 conflict badge prop |
| **H4** | Q8 | F-14 双 context 必须先验证 P3G 模式能力 | ✅ 采纳 — F-14 前置双 context smoke | F-14a smoke + F-14b 主体；验证 localStorage/cookie 隔离 + 独立 seed |

### Medium（7 条）

| # | 触发 | 主题 | 用户拍板 |
|---|---|---|---|
| **M1** | Q1 | F-1 改为"服务端不动"守门切片 | ✅ 采纳 — F-1 重命名为"确认服务端 SaveCanvasResult 不加 appliedToLocal" |
| **M2** | Q2 | F-4 contract test 范围明确 | ✅ 采纳 — F-4 只验证 server/client API shape + MergeReport，hook 层断言放 F-3/F-6 |
| **M3** | Q3 | F-5 必须**冻结** appliedToLocal | ✅ 采纳 — 进 merged 分支立即 `const appliedToLocal = changeSeqRef.current === capturedSeq`；后续只用 frozen boolean |
| **M4** | Q4 | F-6 先于 F-7，F-7 不能太晚 | ✅ 采纳 — F-7 grep 接入 Day 4 验证命令链（不一定进 npm test） |
| **M5** | Q9 | F-15/F-16 顺序应先服务层后路由层 | ✅ 采纳 — F-16 先于 F-15；#34 去掉 g1/g2/g3（已被 F-6/F-7 覆盖） |
| **M6** | Q10 | F-17 末尾审验证命令链 | ✅ 采纳 — 必跑 npm test + tsc 三端 + lint:ids + check:invariants + Day 4 Playwright e2e |
| **M7** | Q12 | D-pre-3 / D-8 / D-12 落点需写进切片 | ✅ 采纳 — F-8 写 Toast 去重能力；F-11 写 deferred 文案 + useRef session once；F-9 写 conflicts undefined fallback 文案 |

### Low（3 条）

| # | 触发 | 主题 | 用户拍板 |
|---|---|---|---|
| **L1** | Q1 | F-11 必须消费 hook SaveResult 不是 API raw | ✅ 采纳 — F-11 明确 case 'merged' 输入类型是 hook SaveResult.merged |
| **L2** | Q5/Q7/Q12 | F-13 "只加注释"价值偏弱 | ✅ 采纳 — F-13 补轻量测试/grep 验证 handleImport / handleSwitchCanvas / beforeunload 都含 conflict 判断 |
| **L3** | Q11 | F-19 部署风险主要是沟通窗口 | ✅ 采纳 — F-19 部署前通知 5 人短暂停止编辑 |

### codex 主动挖出（A1-A2）

| # | 主题 | 用户拍板 |
|---|---|---|
| **A1** | BaseVersionExpiredDialog autosave 语义要单独守住 | ✅ 采纳 — F-10 props 区分 onSaveDraftAndReload 与 onStayInDraft；onStayInDraft 只关弹窗不恢复 autosave |
| **A2** | Toast 去重归属不应全在 ToastHost | ✅ 采纳 — F-11 BFV 持有 deferredMergeToastShownRef，F-8 只提供通用 key replacement |

## 切片重排后的执行顺序

```
F-1 → F-2 → F-3 → F-3.5 → F-4 → F-5 → F-6 → F-7 → F-8 → F-9 → F-10 → F-11 → F-12 → F-13 → F-16 → F-15 → F-14a → F-14b → F-17 → F-18 → F-19 → F-20
```

详见 [02-拍板记录.md § 实施切片清单](02-拍板记录.md#实施切片清单codex-切片设计审重排版)。

## codex 原始报告

> 完整原始返回另存于 `E:/tmp/codex-day4-qiepian-result.md`，关键信息已结构化映射到上述 16 项 + [02-拍板记录.md](02-拍板记录.md) 的切片描述里。

### 整体判定原文

```
canEnterImpl: true
confidence: medium

切片清单是否需要重排：yes

建议顺序：
F-1(no-op 守门) → F-2 → F-3 → F-5 → F-6 → F-7 → F-8 → F-9 → F-10 → F-11 → F-12 → F-13 → F-16 → F-15 → F-14 → F-17 → F-18 → F-19 → F-20

核心调整是：先收口 hook API，再做弹窗集成；先服务层测试债务，再路由层复测；e2e 放在 UI 集成后跑。
```

**Claude 增补**（基于 H1 采纳）：codex 建议顺序在 F-3 后未明示 F-3.5 hook API 封装切片，但 H1 已采纳"加 F-3.5"——因此最终顺序在 codex 建议基础上插入 F-3.5（位置在 F-3 与 F-5 之间，先类型层完成再做命令式封装）。

## 与取舍审的衔接

- 取舍审（[01-取舍审-Day4-真冲突UI.md](01-取舍审-Day4-真冲突UI.md)）：19 项决策锁定，**canEnterImpl: true / confidence: high**
- 切片设计审（本文档）：基于 19 项决策锁定后的实施级审查，**canEnterImpl: true / confidence: medium**
- 16 项调整全部采纳后 confidence 应升回 high — 但需 F-1 ~ F-20 实施完成后由末尾审（F-17）做最终 canEnterBump 判定

## 下一步

- 进切片 1（F-1 服务端守门 → F-2 客户端镜像保持不变 → F-3 SaveResult 收紧 → **F-3.5 hook 命令式封装** → F-4 contract test 范围明确）
- 切片 1 完成后跑 `npm test + tsc 三端 + lint:ids` 验证类型层稳定，再进切片 2 hook 改造
