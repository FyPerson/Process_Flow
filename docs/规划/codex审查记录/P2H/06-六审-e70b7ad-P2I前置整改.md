## 审查结论
- 总体：未通过 P2I gate，2 条必修遗漏 / 1 条新风险
- `save()` discriminated union：部分。返回形态和旧字段清理 OK，但 caller 目前没有真正的 TS exhaustiveness。
- 删除左侧旧保存/导出/导入：OK。没发现用户路径还能触发旧本地文件夹保存；`useFlowOperations` 里的旧回调只是残留死代码。
- `readOnly` 真禁写：失败。ReactFlow 拖拽/连线/Backspace 这层拦了，但大量本地 mutation 入口仍可改画布 UI。

## 必修项
[src/pages/BusinessFlowVisualization/index.tsx](E:/业务全景图/src/pages/BusinessFlowVisualization/index.tsx:74): `switch (result.status)` 没有 default 也没有 `never` 收口，TS 不会在新增 status 时强制报错。`noFallthroughCasesInSwitch` 只管 fallthrough，不管 exhaustiveness。  
具体改法：switch 后加 exhaustiveness check，并修正 [src/hooks/useMultiCanvas.ts](E:/业务全景图/src/hooks/useMultiCanvas.ts:111) 这句注释。

```ts
      switch (result.status) {
        // existing cases
      }

      const _exhaustive: never = result;
      return _exhaustive;
```

[src/components/FlowCanvas/index.tsx](E:/业务全景图/src/components/FlowCanvas/index.tsx:613): `readOnly` 没有包住实际写入口。必须把 `readOnly` 传进或包住 `useFlowHandlers` / `useFlowOperations` / 节点组件 / 详情面板 / 自定义边，至少覆盖这些入口：
- [FlowCanvas index.tsx](E:/业务全景图/src/components/FlowCanvas/index.tsx:725): `Ctrl+V` 仍会 paste；[511](E:/业务全景图/src/components/FlowCanvas/index.tsx:511) `undo/redo` 仍会改本地 nodes/edges；[746](E:/业务全景图/src/components/FlowCanvas/index.tsx:746) `Ctrl+G`、[753](E:/业务全景图/src/components/FlowCanvas/index.tsx:753) `Ctrl+Shift+G` 仍会创建/解散分组。
- [FlowCanvas index.tsx](E:/业务全景图/src/components/FlowCanvas/index.tsx:846): 左侧添加节点按钮仍调用 `onAddNode`；[896](E:/业务全景图/src/components/FlowCanvas/index.tsx:896) 创建分组仍可点。
- [FlowCanvas index.tsx](E:/业务全景图/src/components/FlowCanvas/index.tsx:976): `NodeDetailPanel` 仍拿到可写 callbacks；节点描述/detailConfig/style、边样式、分组属性都能改。
- [useFlowHandlers.ts](E:/业务全景图/src/hooks/useFlowHandlers.ts:393): 边双击删除不走 `handleDelete`，readOnly 下仍可删边。
- [CustomNode index.tsx](E:/业务全景图/src/components/CustomNode/index.tsx:332) 和 [GroupNode index.tsx](E:/业务全景图/src/components/GroupNode/index.tsx:254): `NodeResizer` 仍可调整尺寸。
- [CustomNode index.tsx](E:/业务全景图/src/components/CustomNode/index.tsx:261) / [GroupNode index.tsx](E:/业务全景图/src/components/GroupNode/index.tsx:29): 双击改名仍可进入编辑并通过 `nodeLabelChange/groupLabelChange` 写回。
- [GroupNode index.tsx](E:/业务全景图/src/components/GroupNode/index.tsx:86): 折叠/展开会写 `data.collapsed` 和子节点 hidden 状态。
- [DraggableEdge index.tsx](E:/业务全景图/src/components/DraggableEdge/index.tsx:272): 自定义边拖拽直接 `setEdges` 改 offset，`edgesReconnectable={!readOnly}` 拦不到它。

关键点： [BusinessFlowVisualization/index.tsx](E:/业务全景图/src/pages/BusinessFlowVisualization/index.tsx:355) 的 `handleDataChange` 只是不落父级 project/不触发保存；它不能阻止当前 ReactFlow 本地状态被改，用户会看到“能编辑但刷新/切换后丢失”的假写入。

## 建议项
[CanvasTabBar index.tsx](E:/业务全景图/src/components/CanvasTabBar/index.tsx:317): readOnly 下 no-op 不会写数据，但 add/delete/rename 菜单仍显示，duplicate 菜单也显示但点击无效果。建议给 `CanvasTabBar` 加 `readOnly` prop，保留切换 tab，隐藏或 disabled 增删改复制入口。

[BusinessFlowVisualization/index.tsx](E:/业务全景图/src/pages/BusinessFlowVisualization/index.tsx:474): inline `() => undefined` 不是大问题；`CanvasTabBar` 目前也没 memo 化。后续可以用稳定 no-op 或 readOnly prop 收掉。

## 不必修但要知道
`save()` 的 4 个非 throw 返回路径都已改成 `{ status: ... }`，旧 `result.ok/result.conflict/result.discarded` 没有残留调用方。`save()` 仍会对 `no project/no canvasId/非 409 错误` throw，这和现有 caller catch 是一致的。

旧本地文件夹保存路径只剩 [useFlowOperations.ts](E:/业务全景图/src/hooks/useFlowOperations.ts:143) 的残留 callback，当前没有 UI 或 Ctrl+S 路径能触发。P2I 还不能进，先补 readOnly 全入口禁写和 switch exhaustiveness。