# 文本框节点 MVP - 99-收尾

> 发版日期：2026-05-18
> 版本号：1.19.0 → **1.20.0**（minor bump）
> 立项到发版用时：约 3 小时（含规划文档）
> 方案：[../方案.md](../方案.md)

## 1. 实施结果

✅ **MVP v0.1 完工**：方案 §2.1 范围全部落地，§2.2 非范围严格保留（等业务反馈再加）。

### 1.1 切片完成情况（11 切片）

| # | 切片 | 状态 | 备注 |
|---|---|---|---|
| T-1 | NodeType 加 'text' + TextFontKey + FlowNodeData 3 字段 + FlowDefinition.nodes 3 字段 | ✅ | [src/types/flow.ts](../../../../src/types/flow.ts) |
| T-2 | server schema enum 加 'text' + TextFontKeySchema + NodeSchema 3 optional 字段 | ✅ | [server/schemas/canvas.ts](../../../../server/schemas/canvas.ts) |
| T-3 | TextNode 组件（NodeResizer / 无 Handle / 纯渲染 / FONT_FAMILY_MAP 5 项） | ✅ | [src/components/TextNode/index.tsx](../../../../src/components/TextNode/index.tsx)（80 行） |
| T-4 | FlowCanvas nodeTypes 注册 text: TextNode | ✅ | [src/components/FlowCanvas/index.tsx:46](../../../../src/components/FlowCanvas/index.tsx#L46) |
| T-5 | onAddNode 加 'text' 分支（默认 heiti / 24px / #000000 / 280x40） | ✅ | [src/hooks/useFlowOperations.ts:50-140](../../../../src/hooks/useFlowOperations.ts#L50) |
| T-6 | 侧栏菜单加「+ 文本框」按钮（与起止节点同组） | ✅ | [src/components/FlowCanvas/index.tsx:1122-1136](../../../../src/components/FlowCanvas/index.tsx#L1122) |
| T-7 | NodeDetailPanel 适配文本框（isTextNode 分支 + 精简面板 + 隐废弃 + 隐批注 tab） | ✅ | 新建 [TextNodePropertiesPanel.tsx](../../../../src/components/NodeDetailPanel/TextNodePropertiesPanel.tsx)（170 行） |
| T-8 | schema 7 case 单测（type=text / 3 字段 / 非法 key / size 边界 / 不能做 parent / 可做 group 子） | ✅ | [server/schemas/canvas.test.ts](../../../../server/schemas/canvas.test.ts) +7 case |
| T-9 | tsc 三端 + npm test + lint:ids + check:invariants + check:conflict-guards | ✅ | tsc 三端 0 错 / npm test 576 → **583** +7 全过 |
| T-10 | 手动 PNG 导出验证 | ⏭️ 跳过 | 用户决策：自审 4 条门禁通过 + 内网回滚成本低，部署后用户直接生产实测 |
| T-11 | bump + 99-收尾 + CLAUDE.md 同步 + 双 push + deploy | 🚧 进行中 | 即本文档 |

## 2. 自审 4 条门禁结果

| 门禁 | 检验 | 结果 |
|---|---|---|
| 1. 纯展示组件 | TextNode 无 useState / 无 useEffect / 无 mutation / 仅读 props | ✅ |
| 2. 无 mutation 副作用 | 编辑路径走 NodeDetailPanel → onNodeChange → 上游 setNodes（与所有其他节点同链路） | ✅ |
| 3. 依赖底层已审 | CustomNode / FlowCanvas / useFlowOperations / NodeDetailPanel / NodeResizer 都是阶段 3-5 codex 审过的 | ✅ |
| 4. 边界 ≤ 10 可穷举 | 10 项边界全列举并各自处理（见方案 §7） | ✅ |

**结论**：跳过 codex 审查，走自审通过。

## 3. 指标

| 项 | 前 | 后 | Δ |
|---|---|---|---|
| 版本号 | 1.19.0 | 1.20.0 | minor +1 |
| npm test | 576 | **583** | +7 |
| tsc 错误 | 0 | 0 | - |
| 节点类型数 | 6 | **7**（+ text） | +1 |
| 新增组件 | - | TextNode + TextNodePropertiesPanel | +2 |
| 新增 NPM 依赖 | - | 无 | - |

## 4. 关键决策回顾（方案 §6 + 用户确认）

| # | 决策 | 来源 |
|---|---|---|
| 1 | 档位：MVP（非标准/完整） | 用户确认，§6 决策 1 |
| 2 | 默认样式 A：透明 + 黑字 + 24px + 黑体 | 用户确认，§6 决策 2 |
| 3 | 菜单入口与 process/decision 同组 | 用户确认，§6 决策 3 |
| 4 | 字体清单 5 种系统字体 | 用户确认，§6 决策 4 |
| 5 | 不跑 codex 走自审 | 4 门禁满足，§7 |
| 6 | 不跑本地 Playwright 直接部署 | 用户选 C，本会话 |

## 5. 风险与挑战（方案 §9 vs 实际）

| # | 风险 | 严重度 | 实际 |
|---|---|---|---|
| R1 | html-to-image 字体不嵌入 PNG（5/14 D-4 踩过） | 高 | ⏭️ 待用户生产实测；如有问题改用 SVG `<text>` 直出（方案 §9 已定应对） |
| R2 | TextNode 无 Handle 时拷贝粘贴 / 撤销重做 | 中 | useFlowClipboard 复制路径不区分 node.type，复制后保留 type='text' + textXxx 字段 → 理论正常；待用户实测 |
| R3 | NodeResizer 与无 Handle 兼容 | 中 | 用 React Flow 官方 `<NodeResizer>` 不依赖 Handle 锚定 → 理论正常 |
| R4 | schema enum 加项 vs 旧画布兼容 | 低 | enum 加项不删项，向后兼容；旧画布 schema 重新校验仍通过 |
| R5 | 详情面板分支增多影响可维护性 | 低 | 当前 isGroupNode / isTextNode 2 个特殊分支 + 默认 NodePropertiesPanel；可读 |

## 6. 文档同步（neat-freak 阶段发版三件套）

- ✅ `package.json` bump 1.19.0 → 1.20.0
- ✅ `package-lock.json` 同步
- ➖ `docs/规划/技术债务登记.md` —— **本次无新挂账**（如用户实测发现问题再加）
- ✅ `docs/规划/文本框节点/codex审查/99-收尾.md` —— 即本文件
- ✅ `CLAUDE.md` 当前阶段段落更新（v1.19.0 → v1.20.0 + 文本框节点 MVP 完工）
- ➖ auto memory MEMORY.md —— **本次无新升级**

## 7. 部署信息（待 T-11f 后补完）

| 项 | 值 |
|---|---|
| 本地 commit | _待补_ |
| origin/main | _待补_ |
| server/main | _待补_ |
| 生产 PM2 pid | _待补_ |
| 生产 version | _待补_ |
| /api/health dbWritable | _待补_ |

## 8. 验收口径（方案 §10）

部署完用户实测以下 11 项：

- [ ] 顶栏/侧栏菜单点击「+ 文本框」能添加节点
- [ ] 节点上能看到默认文字"请输入说明文字"
- [ ] 详情面板能修改文字内容、字体（5 选 1）、字号（5 选 1）、颜色（10 色板 + 自定义）
- [ ] 修改后画布立即反映
- [ ] PNG 导出后文字可见 + 字体/字号/颜色正确（**重点验**：5 种字体各导一次 — html-to-image 字体不嵌入是已知风险）
- [ ] 撤销 / 重做 / 拷贝 / 粘贴 / 删除 全正常
- [x] npm test 576 → 583 全过
- [x] tsc 三端 0 错
- [x] lint:ids + check:invariants + check:conflict-guards 全过
- [ ] deploy.ps1 5/5 PASS
- [ ] PM2 真重启 + dbWritable=True + /api/health OK
- [ ] **用户实测在线画布 16 上加文本框 + 调字体字号颜色 + 导出 PNG → 通过**

## 9. 下次迭代方向（若用户实测有反馈）

按方案 §12，按优先级排序：

1. **背景色 + 边框 + 描边**（最接近用户给的"幻灯片标题"样式）
2. **预设模板**（一键插入标题/副标题/脚注/版本号水印）
3. **对齐方式**（左/中/右）
4. **画布上双击行内编辑**（所见即所得）
5. **工具栏快捷按钮**（字号 +/-）

如 PNG 导出字体丢失（R1 风险），改用 **SVG `<text>` 直出** 替代 HTML span 渲染——这是更"基建性"的改造，与上述 5 项可同时排期。
