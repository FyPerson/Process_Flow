# 技术债务登记

**目的**：单点登记所有"已识别但暂不处理"的预存在问题，按建议处理阶段分类。每次 codex 审查归档（17/18/19/20...）末尾凡是出现"挂 step 9 / 挂 P3F / 推迟"的项，都应该回写到这个文件。

**更新约定**：
- 新债务从下面"主动归档"段加入；处理完后**整条移到底部"已偿还"段**，而不是删除（保留追溯性）
- 每条必须含：来源（哪份文档）、问题简述、建议阶段、严重度
- 严重度仅参考：🔴 阻塞下一阶段 / 🟡 影响体验 / ⚪ 代码质量

---

## 主动归档（codex 审查共识：明确推迟到具体阶段）

### ~~挂 P3D-2 step 9~~（已完成 2026-05-05，全部偿还/降级，详见底部"已偿还"段）

> P2J 残留 3/20 Playwright 手动用例（原 #6）保留为独立债务，转移到下方"P2J 残留"段。

### P2J 残留（不属于任何具体阶段）

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 6 | P2J 验收 | Playwright 测试 17/20 自动通过 + **3/20 待手动确认**（验收报告原文：[docs/规划/codex审查记录/阶段2/P2J/](codex审查记录/阶段2/P2J/)） | 🟡 |

### 独立 BUG（暂无法稳定复现，埋点保留生产观察）

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 15 | 2026-05-05 P3E 手工验证实测 | **POST /api/canvases 返回 201 但前端报 network_error**（heisenbug）：实测一次确认服务端创建成功（List API 返回 id=12 "业务流程图_0505" + DevTools Network 显示 201 + content-type application/json），但前端却 alert "创建失败：network_error"。Console 无 fetch 异常 / 同源 fetch 验证均正常 / apiFetch 三处 throw 路径均不应让 201 走 network_error 分支。**当晚加埋点（commit `84cbbe2`）后无法复现**——埋点覆盖 apiFetch 入口/fetch ok/body parsed/returning + createOnServer catch + handleSaveAsNew enter/return/catch；后续测试 id=13 全路径正常（enter → 201 → body parsed → returning → createOnServer returned），无 catch/error 触发。**降级处理**：埋点保留生产代码（代价微小：每次 createCanvas 几条 console.log，对用户无感），等 BUG 再次出现时第一时间有日志定位。原 P0 状态降为 🟡。**id=12 孤儿物证保留**（不删）以备后查 | 🟡 |

### 挂 P3F（admin 后台 UI）

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 9 | 18-七审 / 19-八审 | 中间层包装组件未来可能重新 optional 化 user prop（TypeScript 拦不住） — P3F 加新页面/路由时核对 user 传递链路 | ⚪ |
| 16 | 2026-05-05 P3E 手工验证实测 | **画布列表 UI 入口缺失**：普通用户/admin 浏览/选择服务端画布的 UI 入口缺失，当前只能 URL 拼 `?canvasId=N` 直接打开。新用户登录后看到的是空白本地草稿，不知道有公共画布存在 / 自己之前画布在哪。P3F 共有画布管理需含此入口 | 🟡 |
| 17 | 2026-05-05 P3E 手工验证实测 | **新用户登录后空白草稿状态没有引导**：用户登录后默认看到空白本地草稿（canvasMetaState=local）但**没人告诉用户**"先点'另存到服务器'挂接"或"打开已有画布"。批注 / 多人协作 / 持久化都用不了。P3F 需配套引导文案 / 空状态 hint | 🟡 |
| 18 | 2026-05-05 P3E 手工验证实测（接力 0428 项目重启目标） | **创建用户的 UI 入口缺失**：当前没有创建新用户的 UI 入口，调研窗口期"拉产品岗 + 业务部门关键角色一起画"目标受阻。建议从 P3F 拆出 **P3F-0 创建用户最小入口**先做（半天预估）——admin 后台单页用户创建表单（用户名+密码+角色）+ 用户列表查看，解锁后续验证 | 🟡 |
| ~~19~~ | ~~2026-05-05 P3E 手工验证遗留~~ | ~~**P3E-4 手工验证 6 项中 2 项延后**：游客身份隐藏 resolve 按钮（需无痕窗）+ 普通用户对他人批注隐藏 resolve（需第二普通用户账号）~~ | ✅ **已偿还 2026-05-05 晚段** P3F-0 完成（5 个 user01-05 种子用户 + 临时改 id=12 可见性 public + 用 admin/user01 互验）→ 6/6 全过 → 详见底部"已偿还"段 |
| 20 | 2026-05-05 P3F-1 二审（codex medium 1）| **其他 admin-only 路由仍走旧鉴权**：P3F-1 给 `/api/users` 引入 `requireFreshAdmin`（回查 DB 撤权即时生效），但 `canvases.ts` 里的 admin-only PATCH 等端点仍走 `requireAuth + requireAdmin`——admin 被 P3F-1 降权或软删后旧 admin token 在 token TTL 内仍可访问其他 admin 端点。**接力时机**：P3F-3 admin 后台共有画布管理页接到 canvases.ts admin PATCH 时，一次性扫一遍所有 admin 路由切到 `requireFreshAdmin`（孤立修不经济，配套 P3F-3 自然要看 canvases.ts admin 路径）。**接受期间风险**：内部 5 人项目，"被降权 admin 用旧 token 改自己已有 private 画布"危害低（不放大权限），不是阻塞 P3F-2 进度的风险 | 🟡 |

### 挂 P3G（公共画布发布有摩擦）

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 10 | 19-八审 调用图扫描 | useMultiCanvas.duplicateSheet 复制画布的权限语义未审 — 画布级动作，超出 step 3 节点级 gate 范围；P3G 治理"发布"路径时一并审 | 🟡 |

### 挂 "P3 阶段尾巴"（按方案文档原计划）

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| ~~11~~ | ~~多人协作-方案.md~~ | ~~**P3B** 节点 ID 改 nanoid（5 处全改）~~ | ✅ **已偿还 2026-05-06** P3B 完成（newNodeId/newGroupId/newEdgeId helper + 5 处替换 + lint:ids 兜底 + codex 三轮审查链）→ 详见底部"已偿还"段 |

### 挂 P5（合并算法 4 天 MVP）

阶段 4 codex 4 轮审查链中的 1 medium "业务可接受"风险 + 1 low；接力时机：阶段 5 合并算法落地时一并审视。

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 28 | 2026-05-06 P4 codex 四审 medium | **15s 暂停窗口经验值，非严格互斥**：useMultiCanvas.save() 用 `pauseDraftAutosave(15_000)` 覆盖主版本 PUT + DELETE draft + reset。如果总耗时 > 15s（极慢网络/大草稿），pause 会自然过期，下个 30s tick 仍可能在 reset 前 PUT 复活已删除草稿。**5 人内网下主版本 PUT + DELETE 通常 < 1s，远低于阈值**。**修法**：把 moduleScopedPauseUntilRef 从纯时间戳改成显式 token/计数锁；save 开始 pause，DELETE+reset 完成后 release。P5 合并算法引入显式 lock 时一并改造 | ⚪ |
| 29 | 2026-05-06 P4 codex 四审 low | **DraftStatus 'failed' 混合两类原因**：useDraftAutosave 卸载兜底时若 payload > 60KB 跳过（keepalive 限制），状态用 'failed'；和真实网络/服务端失败混用。文案"草稿保存失败（重试中）"对用户基本可懂，但排查不精确。**修法**：新增 'keepalive_skipped' 状态 + 文案"页面关闭时草稿过大，已等待下次自动保存"；或在 status 上加细分原因字段。P5 重构 DraftStatus 时一并 | ⚪ |
| 30 | 2026-05-06 P5 Day 1 codex 七审 L2 | **schema parentId 测试通过 regex 判失败原因，对文案改动敏感**：[server/schemas/canvas.test.ts](../../server/schemas/canvas.test.ts) 6 case 用 `assert.match(messages, /self-reference/)` 等正则匹配错误消息内容。schema 层 superRefine 文案改了测试就脆性失败。**修法**：抽错误消息常量（如 `MSG_PARENTID_SELF_REFERENCE`），或改用 `path/success === false` 等更稳定的断言。**接力时机**：P5 Day 2-3 写 detector 测试时若发现同款脆性，统一抽常量 | ⚪ |
| 31 | 2026-05-06 P5 Day 1 codex 五审 L1 | **静态断言不约束 NodeSchema Zod shape 本身**：types.ts 的 `_NodeDiffFieldsForwardAssert / BackwardAssert` 仅约束 NODE_DIFF_FIELDS 与 StorageNodeContentFields 双向覆盖；如果未来只在 schema NodeSchema 加字段、忘了同步 StorageNodeContentFields/NODE_DIFF_FIELDS，编译仍可能不报错。CONNECTOR 同款。**修法**：派生 NodeSchema/EdgeSchema 字段名常量，写单测对照 schema shape key 与 StorageContentFields/DIFF_FIELDS 三者差异。**接力时机**：P5 之后任何"代码质量子阶段"或新增 storage 字段时统一处理 | ⚪ |
| 32 | 2026-05-07 P5 Day 2 阶段 B-2 codex 复审 L2 | **N9 双端点缺失只产 1 条 conflict（丢 target 信息）**：[server/services/merge/detector.ts:347-364](../../server/services/merge/detector.ts) 中 detectEdgeConflicts 对"加新边引用对方已删节点"按 source 优先 + continue，若 source 和 target 同时被对方删则只记 source 缺失。**当前可接受**：5 人内网阻断合并层面 1 条足够，弹窗给出第一个缺失端点已可让用户决策。**修法**：B-4 conflict_logs 实施时改为 details 字段携带完整端点信息（按端点产 2 条 / 或 1 条 message+details），同时单测加双端点都被删 case。**接力时机**：B-4 tryMerge + conflict_logs 实施 | ⚪ |
| 33 | 2026-05-07 P5 Day 2 阶段 D 末尾审 test_gap 1+2 | **PUT /api/canvases route 层 DataIntegrityError + conflict 序列化未直接复测**：阶段 C 7 case 在 service 层断言 `saveCanvas throws DataIntegrityError` / `result.error='conflict'`，但 route 层 catch 块映射 `500 + 'data_integrity_error'` + 409 + conflicts 数组 JSON 序列化没经 supertest 直接复测。**当前可接受**：route 层 catch 块 [server/routes/canvases.ts:262-291,306-317](../../server/routes/canvases.ts) 阶段 A 已就位 + 单元层 throws 已锁；端到端缺失只是"双重保险"未配齐。**修法**：Day 4 Playwright e2e 一并加 supertest case 覆盖 PUT route 层 → 验证 status code + JSON body 字段 | ⚪ |
| 34 | 2026-05-07 P5 Day 2 阶段 D 末尾审 test_gap 4 | **合并端到端测试遗漏路径**：阶段 C 7 case 覆盖了"真合并/真冲突/active_sheet_missing/权限优先级/saved_by/DataIntegrityError"6 项验收清单，但**未覆盖**：(a) annotations 表的级联删除（合并路径删 sheet/node 时），(b) 副作用型 sheet 删除（B 删 sheet 同时 A 改该 sheet 内节点 → sheet_removed_modified 真冲突），(c) deprecated/alsoDeprecated 路径合并，(d) E7 warnings 流入 mergeReport，(e) connector 自动合并路径（E1/E2/E3）。**当前可接受**：detector + applyDelta 单元层各自已覆盖（B-1/B-2/B-2.5/B-3 总 60+ case），saveCanvas 端到端只缺集成层断言。**修法**：Day 3-4 客户端实施 + Playwright e2e 时一并加 saveCanvas 集成层 case（按场景 5-8 个） | ⚪ |

### 工程基建（不阻塞下一阶段）

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| ~~21~~ | ~~2026-05-06 P3B codex 二审 #5~~ | ~~**测试文件无独立 type-check**~~ | ✅ **已偿还 2026-05-06 P3B 收尾** 新建 [tsconfig.test.json](../../tsconfig.test.json)（include src+server，类型检查覆盖测试 + 源代码引用链），加 `npm run typecheck:test` 接到 `npm test` 前置，根 tsconfig.json references 同步更新；同时根治了 IDE 打开 *.test.ts 显示 `node:test` 找不到的红波浪线 |

### 安全 / 工程债（来源：项目潜在风险清单 4-28，对账 5-06 仍未偿还）

> 本项目 4-28 出过一份独立"项目潜在风险清单"（已并入本登记后归档删除）。原 12 项里 6 项已根治（生产 HMR / package-lock 跟踪 / localStorage 节流 / 复制粘贴 remap / 导入 schema / settings.local.json 排除），下面是仍未偿还的 6 项。**接力时机不强制**，等"代码质量子阶段"或安全审计专项时一并处理。

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 22 | 项目潜在风险清单 4-28 #1 | **隐藏子流程可能被自动覆盖丢失**：BFV `showSubflows=false` 默认过滤 type==='subprocess' 节点，过滤后的 processedNodes/Edges 直接喂 FlowCanvas → onDataChange → 写回 localStorage / 服务端。一旦数据中出现子流程节点，可能把"过滤后的视图"当真实画布数据保存，永久丢失。**修法**：FlowCanvas 接收完整数据，hidden 用渲染层处理；onDataChange 区分"视图过滤"和"真实数据"。**接力时机**：当 subprocess 类型真被用起来（目前未启用 → 风险潜伏） | 🔴 |
| 23 | 项目潜在风险清单 4-28 #2 | **iframe 同源未隔离 + 静态页面 innerHTML 注入**：主应用同源 iframe 加载多个静态 HTML（src/App.tsx 9 处 + ScreenshotViewer + ScreenshotManager），未设 sandbox；public/pages/施工班组/收入合同 部分页面把输入拼接 innerHTML。形成同源 XSS 风险。**修法**：iframe 加最小权限 sandbox；静态页面改 textContent / DOM API；评估 allow-scripts/allow-forms 必要性。**接力时机**：内部 5 人项目接受度高，但若有公网暴露/陌生用户接入需立即处理 | 🟡 |
| 24 | 项目潜在风险清单 4-28 #4 | **依赖漏洞 + NODE_TLS_REJECT_UNAUTHORIZED=0**：4-28 npm audit 报 3 个漏洞（react-router 7.9.6 high / lodash 4.17.21 high via dagre→graphlib），且环境关 TLS 校验。**修法**：恢复 TLS 校验；升级 react-router-dom；评估替换 dagre 或 lodash 覆盖；CI 加 `npm audit --omit=dev --audit-level=high`。**接力时机**：5-06 现状待重新跑 audit 确认（可能已部分缓解）；安全审计专项时统一处理 | 🟡 |
| 25 | 项目潜在风险清单 4-28 #5 | **hook 全局 safe.directory '*' + reset --hard 边界过宽**：server-hooks/post-receive.ps1 用 `git config --global --add safe.directory '*'`（影响整机 git 信任边界）+ `reset --hard` / `clean -fd`（部署目录手工修复会被强制覆盖）。hook v2 已收 ssh 但这两条没动。**修法**：safe.directory 改白名单具体路径；明确部署目录不可手工修改；hook 模板纳入版本控制做漂移检测 | 🟡 |
| 26 | 项目潜在风险清单 4-28 #7 | **Lint 失败 + 含 React 行为级问题**：4-28 npm run lint 报 20 errors / 52 warnings，含 react-hooks/refs（FlowCanvas:179/183 渲染期写 ref）/ exhaustive-deps（useFlowOperations:386 缺 edges/saveHistory）/ preserve-manual-memoization / effect 内同步 setState（GroupNode:20/25）。状态同步可能用过期闭包，未来 React/ESLint/Compiler 升级易暴露。**修法**：优先修 react-hooks/* 类规则；表单和外部数据同步做受控拆分；修完后 lint 加进 CI 阻断线 | 🟡 |
| 27 | 项目潜在风险清单 4-28 #12 | **缺 CI**：5-06 已有 228 单测 + Playwright + lint:ids + typecheck:test，但无 GitHub Actions / 任何远端 CI。本地 npm test 是手动门禁，push 后无强制阻断。**修法**：加 .github/workflows/ci.yml 跑 npm ci + lint:ids + typecheck:test + npm test + npm audit；PR 不过 CI 不能 merge。**接力时机**：当多人提 PR 时（目前主要单人开发，重要性偏低） | ⚪ |

---

## 被动累积（lint / 类型债，建议独立"代码质量子阶段"处理）

**建议时机**：P3 阶段全部完成后、P4 心跳前插入 0.5-1 天

| # | 来源 | 问题 | 严重度 |
|---|---|---|---|
| 12 | npm run lint | **74 个 lint 问题（20 errors + 54 warnings）**，35 处 `as any` 散落在 9 个文件 | ⚪ |
| 13 | useFlowOperations.ts:8 处 | useFlowOperations.ts 内集中度最高的 `as any`（最优先清理） | ⚪ |
| 14 | useFlowOperations.ts:553-571 | onUpdateGroupLabel 函数前面 17 行历史思考注释（"我们可以采取两步走"），是迭代过程中遗留的"自言自语"，无价值 — 八审 M1 修法时**已经顺手删了** | ✅ 已偿还（八审）|

> 注：14 已经在八审 M1 修法时一并清理了，保留在此作为示例。下次清理代码注释时也参考这种"顺手做"风格。

---

## 已明确阶段的延期项（方案文档已声明，等阶段轮到时做）

> 这些不算"债务"，是按计划走，登记仅为完整性。详见 [多人协作-方案.md](./多人协作-方案.md) 第 7 节实施计划。

| # | 阶段 | 内容 |
|---|---|---|
| - | P3D-2 step 4 | 详情面板权限禁用 |
| - | P3D-2 step 5-8 | 剩余 mutation 入口（按方案文档原计划） |
| - | P3D-2 step 9 | 测试 + 重构 + 回归（接力上面 1-8 项） |
| - | P3E | 批注 CRUD + 气泡 UI |
| - | P3F | admin 后台 UI |
| - | P3G | 公共画布"发布有摩擦"治理 |
| - | P4 | 心跳 + auto-save |
| - | P5 | 合并算法 |

---

## 已偿还（保留追溯）

| # | 来源 | 问题 | 处理时间 / 阶段 |
|---|---|---|---|
| 14 | 八审 M1 顺手 | useFlowOperations.ts:553-571 onUpdateGroupLabel 前的 17 行历史思考注释 | 2026-05-04 八审 M1 修法时清理 |
| 1 | 12-代码审查 二审 | useFlowHistory undo/redo 整份 snapshot 恢复绕过中心 gate | 2026-05-05 step 9：mergeSnapshotByPermission + mergeEdgesForMergedNodes 双 helper + parentId/group嵌套归一化（codex 三轮审通过，[26](codex审查记录/阶段3/P3D/26-代码审查-P3D-2-step9-三项修法.md) / [27](codex审查记录/阶段3/P3D/27-二审-P3D-2-step9-三项修法.md) / [28](codex审查记录/阶段3/P3D/28-三审-P3D-2-step9-三项修法.md)） |
| 2 | 17-六审 medium3 | position/dimensions NaN/Infinity 数值校验 | 2026-05-05 step 9：isValidXY / isValidDimensions / isValidStyleGeometry helper + filterNodeChangesByPermission 全分支接入 + canApplyNodeUpdate style 校验 |
| 3 | 19-八审 / 20-九审 Low 1 | useFlowHistory.saveHistory 在 React 18 StrictMode 双调用下不幂等 | 2026-05-05 step 9：saveHistory 内 snapshotsEqual 栈顶判等去重 |
| 4 | 18-七审 L1 / 19-八审 L2 / 20-九审 Low 2 | onNodeUpdate updatedStyle = {} RTL 行为锁定 | 2026-05-05 step 9：helper 单测已在 27-二审 low 1 修法时覆盖（style 非 object fail-closed），原占位降级见 [29-自审-P3D-2-step9-RTL占位清单降级](codex审查记录/阶段3/P3D/29-自审-P3D-2-step9-RTL占位清单降级.md) |
| 5 | 18-七审 / 19-八审 自审 | useFlowClipboard.pasteNodes RTL 验证 | 2026-05-05 step 9：降级为自审 checklist + 部署后人工回归（同 #4 归档） |
| 7 | 20-九审 Medium 占位 | onNodeUpdate ghost nodeId 早退 RTL 集成测 | 2026-05-05 step 9：降级（同 #4 归档） |
| 8 | 19-八审 M1 占位 | onUpdateGroupLabel 拒绝路径副作用 RTL 集成测 | 2026-05-05 step 9：降级（同 #4 归档） |
| 19 | 2026-05-05 P3E 手工验证遗留 | P3E-4 手工验证 6 项中 2 项延后（游客身份隐藏 resolve / 普通用户对他人批注隐藏 resolve）| 2026-05-05 晚段：P3F-0 创建 5 个种子用户（user01-05 / 密码 12345678）+ 临时改 id=12 visibility=public+is_public_to_guest=1 → 用无痕窗（游客）+ user01/admin 互验 → 6/6 全过 → 改回 id=12 visibility=private |
| 11 | 多人协作-方案.md | **P3B** 节点 ID 改 nanoid（5 处全改）| 2026-05-06：newNodeId/newGroupId/newEdgeId helper（nanoid customAlphabet）+ 5 处替换 + remapClipboardNodes 纯函数（按 type 分派 g_/n_、parentId/position remap、relatedNodeIds 过滤、data.id 同步、name/label 去重）+ scripts/check-id-generation.mjs lint 兜底 + codex 4 轮审查链 confidence=high → v1.10.0 |
| 21 | 2026-05-06 P3B codex 二审 #5 | **测试文件无独立 type-check** | 2026-05-06：新建 [tsconfig.test.json](../../tsconfig.test.json)（include src+server，类型检查覆盖测试 + 源代码引用链）+ 加 `npm run typecheck:test` 接到 `npm test` 前置 + 根 tsconfig.json references 同步；同时根治了 IDE 打开 *.test.ts 显示 `node:test` 找不到的红波浪线 |

---

## 维护说明

- **新增**：codex 审查归档写完后，看末尾"挂 step 9 / 挂 P3F" 之类的项，立刻回写到本文件"主动归档"段。
- **偿还**：处理完一项时，把整条从"主动归档"或"被动累积"段移到"已偿还"段，加处理时间。
- **不删除**：偿还后保留在"已偿还"段，方便未来复盘"这个问题当时是怎么发现 / 怎么解决的"。
- **入口检查**：每次 step 完成时，盘问一句"这一 step 引入的延期项归档了吗"——避免散落在多份 codex 审查文档里要再去爬。
